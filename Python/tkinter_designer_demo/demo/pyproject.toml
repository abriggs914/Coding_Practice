[tool.poetry]
name = "demo"
version = "0.1.0"
description = ""
authors = ["Avery <abriggs1@unb.ca>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
tkdesigner = "^1.0.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
